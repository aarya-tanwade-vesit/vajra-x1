Ok so my friend Ananya is building the frontend using v0. Write a .txt page as prompt to send her. Keep it very detailed and explain about the project in detail, act as a professional developer. Assume that it doesnt has any context which it really doesnt - tell it about hackathon wagera. Don't tell it with exactness on 'how and what colors and theme let it choose first, but generally we want clean and amazing page like we made in AeroGuard if u remember wesa hi chiye

First will be Landing Page, then onboarding example (act like we're service providers, also im going to make an gmail id for our AI like vajra.x1@gmail.com get its 'app password' that 16 digit one - then in the onboarding we'll tell the user (idk who'll be the user maybe the factory floor maanger or something ask it to enter a email address to send alrets, then all the features i mentioned - Here is the comprehensive, detailed list of all the features that will be in your Vajra X1 project. I have broken them down logically so you know exactly what is happening in each part of your system.1. IoT Data Simulation & Streaming PipelineThis is the foundation of your project, proving that your system can handle real-world factory data.Simulated Multi-Node Environment: Generates synchronized time-series data for three distinct, interconnected CNC machines (Node L, Node M, Node H).Cascading Failure Engine: Custom mathematical logic where one machine's failure (e.g., Node L overheating) directly impacts the data of interconnected machines (e.g., Node M's torque spikes to compensate).Signal Jitter & Degradation: Applies realistic Gaussian noise to baseline sensor readings, followed by linear degradation and exponential spikes to simulate true mechanical wear and tear.Real-Time MQTT Integration: Instead of just reading a static CSV, the data is continuously broadcasted via the MQTT protocol to perfectly mimic live industrial IoT sensors.2. Predictive Machine Learning Engine (The Brain)This is where your data science skills shine, moving beyond basic classification to a true early-warning system.Primary Classification Stack: An active training pipeline evaluating Random Forest, XGBoost, CatBoost, and LightGBM to detect the base probability of a machine failure.Probability Calibration Layer: A crucial algorithm that smooths out extreme machine learning probabilities (like 0.999 or 0.0001) into a realistic, gradual, and human-readable percentage.Dynamic Risk Scoring (0-100%): A composite metric that combines the calibrated failure probability with a "stress penalty" calculated from the cascading logic.Dual-Layer Anomaly Detection (In Progress): Integration of a secondary model (like Local Outlier Factor) to run in parallel with the classifier, catching sudden, unknown deviations before they become full failures.3. Interactive Digital Twin Dashboard (The Face)Built using Streamlit, this is what the judges will interact with during your pitch.Live Sensor Telemetry: Real-time line charts displaying incoming data (Temperature, Torque, RPM, Tool Wear) ticking across the screen.Dynamic Network Graph Visualization: A visual map of the interconnected machines (Nodes). As a machine's Risk Score rises, its node changes color (Green $\rightarrow$ Yellow $\rightarrow$ Red), and the stress transfers along the connected edges."What-If" Stress Simulator: An interactive control panel with sliders. Judges can manually inject stress (e.g., "Increase Global Ambient Temperature by 15°C") and watch the network graph instantly react and reroute the risk.Smart Categorized Alerts: A notification log that translates raw scores into actionable maintenance advice (e.g., Safe (0-30%), Warning: Monitor Torque (31-70%), Critical: Impending Heat Failure (71-100%)).4. Scalable System ArchitectureThis proves your hackathon project could actually be deployed in a real factory.Edge-Computing Design Pattern: The system is architected so that the heavy ML processing happens "on the edge" (acting as a local Raspberry Pi) rather than sending all raw data to the cloud.FastAPI Bridge: A lightweight REST API that seamlessly serves the processed Risk Scores and node statuses from the backend ML engine to the frontend Streamlit dashboard.. 3. The Financial "Cost of Downtime" Ticker

The Idea: Technical judges care about accuracy; business judges care about money. Translate your ML predictions into financial impact.

How it works: Add a widget to your Streamlit dashboard that calculates the cost. If Node L fails, the whole line stops.

The Output: Show a live counter: "Preventative Fix Cost: ₹5,000 | Predicted Catastrophic Failure Cost: ₹150,000/hour. Action saves ₹145,000."

5. Technician's "Mobile AR" QR Code

The Idea: Show that you thought about the actual factory workers on the floor.

How it works: Put a dynamic QR code on your main Streamlit dashboard next to each machine node.

The Pitch: During your presentation, ask the judges to scan the screen with their phones. It opens a clean, mobile-friendly web page showing that specific machine's live stats, simulating the "companion app" a technician would use while walking the factory floor.

Also email se report download krne ka option in one click


Also a separate page for AI Report like we made for Aeroguard. I think u have the notebook so give the real data right now of the AI meterics or badme bhi kr skte hai


1. The "Green Tech" ESG Monitor (Sustainability Angle)The Idea: Failing machines work harder, generate more friction, and draw more electricity. Turn this into a sustainability feature.How it works: You already have Torque and Rotational Speed [rpm] in your data. You can easily calculate mechanical power ($Power = Torque \times Speed$).The Output: Add a "Wasted Energy & Carbon Footprint" widget to the dashboard. Show how much extra CO2 the factory is emitting because Node L is running inefficiently. Judges love projects that tie into green technology and sustainability.


4. The "Post-Mortem" Snapshot Generator

The Idea: When a machine breaks, engineers need to know exactly what happened in the 10 minutes leading up to the failure.

How it works: If a machine's Risk Score hits 100%, trigger a function that grabs the last 50 rows of data for that specific machine and saves it as a neat PDF or CSV "Incident Report."

The Output: A button appears on the dashboard saying "Download Failure Snapshot." It shows you thought about the forensic data engineering side of the problem. - cab be added in email


5. Live "Radar" Explanation (XAI)

The Idea: Instead of just outputting "90% Risk", visualize why the model is panicking in a really cool way.

How it works: Use a Radar/Spider chart component in Streamlit. The axes are your features (Temp, RPM, Torque, Tool Wear).

The Output: When the machine is healthy, the radar shape is a small, tight circle in the center. As the machine degrades, the "Torque" point stretches way out to the edge of the web. It provides instant, visual Explainable AI (XAI) without needing complex SHAP values.


Also we can show which part of the CNC machine is failed based on the failure mode
